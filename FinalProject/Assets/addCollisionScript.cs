using UnityEngine;
using System.Collections;

public class addCollisionScript : MonoBehaviour {
	public AudioClip bump;

	// Use this for initialization
	void Start () {
//		foreach (Transform child in transform){
//			child.gameObject.AddComponent<bumpCollision>();
//			AudioSource bumpSound = child.gameObject.AddComponent<AudioSource>();
//			audioSound = GetComponentsInChildren<AudioSource> ().clip;
//			audioSound = bump;
//
//		}
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
